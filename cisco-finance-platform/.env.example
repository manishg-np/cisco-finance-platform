# Copy this file to .env and fill in your Anthropic API key.
# The app's AI features (AI narrative, AI Hub tab) call the Anthropic API.
# The Vite dev server proxies these requests, so your key stays server-side.

ANTHROPIC_API_KEY=sk-ant-...
